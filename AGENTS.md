# Agent Instructions

## Build & Execution
- Always target the `src` directory when running `dotnet` commands (e.g., `dotnet build src`, `dotnet test src`).

## Formatting & Linting
- Always run `dotnet format src` before building or testing after any file edits.
